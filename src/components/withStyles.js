import React from "react";

// IGNORE THIS FILE

// POC
const withStyles = (Component) => {
  return <Component style={{}} />;
};

// export default withStyles;

const Navbar = ({ props }) => <nav></nav>;

export default withStyles(Navbar)({ padding: "5px" });
